<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.external.mapper.HRfidInfoMapper">



    <!-- 根据rfid标签获取库存信息 -->
    <select id="queryHrfidList" resultType="java.util.Map">
        SELECT DISTINCT
        st.product_Id AS "productId",
        st.product_no AS "productNo",
        st.batch_no  AS "batchNo",
        st.valid_date  AS "validDate",
        st.product_bar_code AS "productBarCode",
        st.stock_num  AS "stockNum",
        st.id  AS "kcId",
        st.product_name  AS "productName",
        prod.spec  AS "spec",
        prod.version  AS "version",
        prod.pru_price AS "inPrice",
        prod.selling_price AS "outPrice"
        from h_rfid_info rfid,
        pd_product_stock st,
        pd_product prod
        <where>
            rfid.productNo=st.product_no
            and rfid.productId=st.product_Id and rfid.batchNo=st.batch_no
            and rfid.productId=st.product_id and rfid.productId=prod.id
            <if test="rfId != null and rfId != ''">
                AND rfid.rfId=#{rfId}
            </if>
            <if test="productNo != null and productNo != ''">
                AND rfid.productNo=#{productNo}
            </if>
            <if test="kfId != null and kfId != ''">
                AND st.storeroom_id=#{kfId}  and st.stock_num>0
            </if>
            <if test="rfIds != null and rfIds.size() > 0">
                and rfid.rfId IN
                <foreach collection="rfIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="batchNos != null and batchNos.size() > 0">
                and rfid.batchNo IN
                <foreach collection="batchNos" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>

    <!-- 获取rfid标签信息 -->
    <select id="queryHrfid" resultType="java.util.Map">
        SELECT
        rfid.id AS "id",
        rfid.rkmxid AS "rkmxId",
        rfid.productNo AS "productNo",
        rfId.productId AS "productId",
        rfId.isDisable AS "isDisable",
        rfId.batchNo AS "batchNo"
        from h_rfid_info rfid
        <where> 1=1
            <if test="rfId != null and rfId != ''">
                AND rfid.rfId=#{rfId}
            </if>
            <if test="productNo != null and productNo != ''">
                AND rfid.productNo=#{productNo}
            </if>
            <if test="productId != null and productId != ''">
                AND rfid.productId=#{productId}
            </if>
            <if test="batchNo != null and batchNo != ''">
                AND rfid.batchNo=#{batchNo}
            </if>
            <if test="rfIds != null and rfIds.size() > 0">
                and rfid.rfId IN
                <foreach collection="rfIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="batchNos != null and batchNos.size() > 0">
                and rfid.batchNo IN
                <foreach collection="batchNos" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>


</mapper>